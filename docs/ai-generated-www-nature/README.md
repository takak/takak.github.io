# 🌳 AI Generated WWW Nature

毎日成長する自然を、AI（Claude）が生成し、GitHub Pagesで公開するプロジェクトです。

## 📅 プロジェクト概要

- **開始日**: 2026年1月1日（冬）
- **実行時刻**: 毎日 JST 3:00
- **解像度**: 中解像度（4px、200×150グリッド）
- **季節**: 日本の四季に対応（12-2月=冬、3-5月=春、6-8月=夏、9-11月=秋）

## 🎨 特徴

### 二層構造の時間軸

1. **長期的成長**（年単位、循環しない）
   - 🌳 木の幹と葉が年々成長
   - 🪨 岩に苔や地衣類が増える
   - 🍄 キノコのコロニーが拡大
   - 🪵 古い木が倒れて分解
   - 🏞️ 土壌が肥沃化

2. **季節サイクル**（365日で循環）
   - 🍃 葉は春に芽吹き、冬に落ちる
   - 🌿 草の高さと色が変化
   - 🌸 花は春夏に咲き、秋冬に枯れる
   - 🦋 動物の活動も季節で変化

## 🚀 セットアップ

### 1. リポジトリの準備

```bash
# リポジトリをクローン
git clone <your-repo-url>
cd nature-garden

# 依存関係をインストール
cd scripts
npm install
cd ..
```

### 2. GitHub Secrets の設定

GitHub リポジトリの Settings > Secrets and variables > Actions で以下を設定：

- `ANTHROPIC_API_KEY`: あなたの Anthropic API キー

### 3. GitHub Pages の有効化

1. Settings > Pages
2. Source: "GitHub Actions" を選択
3. 保存

### 4. 初回実行（手動）

```bash
# Actions タブで "Daily Nature Growth" を手動実行
# または、以下のコマンドで初日を生成
cd scripts
node calculate-season.js
node generate-scene.js
node create-day-page.js 2026-01-01
node update-index.js
```

## 📂 ディレクトリ構造

```
nature-garden/
├── .github/
│   └── workflows/
│       └── daily-growth.yml        # GitHub Actions ワークフロー
├── days/
│   └── YYYY-MM-DD/                 # 各日のディレクトリ
│       ├── index.html              # その日のページ
│       └── scene.json              # シーンデータ
├── shared/
│   ├── nature-engine.js            # 描画エンジン
│   ├── season-colors.js            # 季節の色パレット
│   └── timeline.js                 # タイムライン機能
├── scripts/
│   ├── calculate-season.js         # 季節計算
│   ├── generate-scene.js           # Claude API でシーン生成
│   ├── create-day-page.js          # 日次ページ作成
│   ├── update-index.js             # インデックス更新
│   ├── initial-scene.json          # 初期シーン
│   └── package.json                # 依存関係
├── index.html                      # トップページ（自動生成）
└── README.md                       # このファイル
```

## 🎮 使い方

### GitHub Pages で閲覧

- URL: `https://<username>.github.io/<repository-name>/`
- 毎日 JST 3:00 に自動更新されます

### ローカルで確認

```bash
# 簡易サーバーを起動
python3 -m http.server 8000
# または
npx serve .

# ブラウザで開く
open http://localhost:8000
```

### タイムライン機能

トップページでは：
- スライダーで任意の日に移動
- ▶️ ボタンで1秒間隔で再生
- ⏮️⏪⏩⏭️ で日付を移動

## 🛠️ カスタマイズ

### 実行時刻の変更

`.github/workflows/daily-growth.yml` の cron 設定を変更：

```yaml
schedule:
  # JST 3:00 = UTC 18:00 (前日)
  - cron: '0 18 * * *'
```

### 解像度の変更

`scripts/initial-scene.json` と `scripts/generate-scene.js` の解像度設定を変更。

### 季節の定義変更

`scripts/calculate-season.js` の `getSeasonFromMonth()` 関数を編集。

## 📊 データ構造

### シーンデータ

```json
{
  "metadata": {
    "dayNumber": 1,
    "cycleYear": 1,
    "season": "冬"
  },
  "permanentElements": {
    "trees": [...],
    "rocks": [...],
    "shrubs": [...]
  },
  "seasonalElements": {
    "leaves": [...],
    "grasses": [...],
    "flowers": [...]
  },
  "ecosystem": {
    "maturityLevel": 0.1,
    "soilQuality": 0.3
  }
}
```

詳細は `EXTENDED_GROWTH_SYSTEM.md` を参照。

## 🐛 トラブルシューティング

### GitHub Actions が失敗する

1. `ANTHROPIC_API_KEY` が正しく設定されているか確認
2. API クォータが残っているか確認
3. Actions のログを確認

### ページが表示されない

1. GitHub Pages が有効になっているか確認
2. リポジトリが Public か確認
3. `index.html` が生成されているか確認

## 📝 ライセンス

MIT License

## 🙏 謝辞

- [Anthropic Claude](https://www.anthropic.com/) - AI による自然生成
- [GitHub Pages](https://pages.github.com/) - ホスティング
- [GitHub Actions](https://github.com/features/actions) - 自動化

---

**Enjoy watching nature grow! 🌱→🌳**
